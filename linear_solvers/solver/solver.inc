C -*- Fortran -*-

C     Do not clear memory
      INTEGER NOINIT
      PARAMETER(NOINIT=0)

C     Maximum positive 32 bit integer
      INTEGER MAXINT
      PARAMETER(MAXINT=2147483647)

C     Direct Solver names
      INTEGER SOLV_LU,SOLV_LSQ,SOLV_SVD,SOLV_CHOLESKY,SOLV_LDL
      PARAMETER(SOLV_LU=1)
      PARAMETER(SOLV_SVD=2)
      PARAMETER(SOLV_LSQ=3)
      PARAMETER(SOLV_CHOLESKY=4)
      PARAMETER(SOLV_LDL=15)
      INTEGER SOLV_SUPERLU,SOLV_UMFPACK4,SOLV_UMFPACK2,SOLV_HARWELL
      INTEGER SOLV_SPARSELU
      PARAMETER(SOLV_SPARSELU=20)
      PARAMETER(SOLV_SUPERLU =SOLV_SPARSELU+1)
      PARAMETER(SOLV_UMFPACK4=SOLV_SPARSELU+2)
      PARAMETER(SOLV_UMFPACK2=SOLV_SPARSELU+3)
      PARAMETER(SOLV_HARWELL =SOLV_SPARSELU+4)

C     Iterative Solver names
      INTEGER SOLV_JACOBI,SOLV_SOR
      PARAMETER(SOLV_JACOBI=5)
      PARAMETER(SOLV_SOR=6)
      INTEGER SOLV_IC0,SOLV_IC1,SOLV_ILU0,SOLV_ILU1,SOLV_ILDL0
      INTEGER SOLV_ILDL1
      PARAMETER(SOLV_IC0=12)
      PARAMETER(SOLV_IC1=13)
      PARAMETER(SOLV_ILU0=7)
      PARAMETER(SOLV_ILU1=8)
      PARAMETER(SOLV_ILDL0=18)
      PARAMETER(SOLV_ILDL1=19)
      INTEGER SOLV_CG,SOLV_BICGSTAB,SOLV_GMRES
      PARAMETER(SOLV_CG=9)
      PARAMETER(SOLV_BICGSTAB=10)
      PARAMETER(SOLV_GMRES=11)
      INTEGER SOLV_AMG,SOLV_BMG,SOLV_PCG_BMG
      PARAMETER(SOLV_AMG=14,SOLV_BMG=12,SOLV_PCG_BMG=13)

C     Preconditioner names
      INTEGER PRE_NONE,PRE_POINT,PRE_JACOBI,PRE_SOR,PRE_IC0,PRE_ILU0
      INTEGER PRE_IC1,PRE_ILU1,PRE_ILDL1,PRE_ILDL0,PRE_SCALE
      PARAMETER(PRE_NONE=1)
      PARAMETER(PRE_POINT=2)
      PARAMETER(PRE_JACOBI=3)
      PARAMETER(PRE_SOR=4)
      PARAMETER(PRE_ILU0=5)
      PARAMETER(PRE_ILU1=6)
      PARAMETER(PRE_SCALE=7)
      PARAMETER(PRE_IC0=8)
      PARAMETER(PRE_IC1=9)
      PARAMETER(PRE_ILDL0=10)
      PARAMETER(PRE_ILDL1=11)
