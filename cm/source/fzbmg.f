C**** CMISS Module FZBMG.F: Dummy BMG routines


      SUBROUTINE BMG_SET_parms(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_PCG_SET_parms(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_SymStd_SETUP_PtrWork(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_Free(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_Alloc(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_UTILS_iV_zero(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_UTILS_rV_zero(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_CONSTRUCT_CM_IJK_map(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_CONVERT_csr_matrix(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_IJK_map_size(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_SETUP(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_CONVERT_rhs(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_CONVERT_soln(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_SOLVE(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_PCG_SOLVE(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END

      SUBROUTINE BMG_PCG_setup(A,*)
      DIMENSION A(*)
      WRITE(*,*) '>>Link with boxmg library'
      RETURN 1
      END
